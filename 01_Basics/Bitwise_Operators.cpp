#include <iostream>

using namespace std;

int main(){

    //Bitwise Operators Operate at the bit level,
    //such as AND (&), OR (|), XOR (^), left shift (<<), and right shift (>>).

    int a = 4;
    int b = 2;
    float c = 100000000;



    cout<<(a&b)<<endl;

    // sizeof() ; to get the size of the operator

    cout<<"Size of the a:"<<sizeof(a)<<endl;
    cout<<"Size of the b:"<<sizeof(b)<<endl;
return 0;
}


